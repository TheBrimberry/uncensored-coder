# Core dependencies
ollama>=0.1.0
requests>=2.31.0
pyyaml>=6.0

# Interface
rich>=13.0.0
prompt-toolkit>=3.0.0

# Optional
pytest>=7.4.0
black>=23.0.0

# --- Trading agent (all optional; the agent degrades gracefully without them) ---
# Market data
# yfinance>=0.2.0          # stocks / etf / fx / indices
# ccxt>=4.0.0              # crypto exchanges (data + execution)
# pandas>=2.0.0            # optional, speeds up data handling
# Brokers / execution
# MetaTrader5>=5.0.0       # FX / "4X" / CFDs (Windows + MT5 terminal)
# tradelocker>=0.1.0       # TradeLocker multi-asset brokers
# alpaca-py>=0.20.0        # US stocks / crypto (paper + live)
# Research / ML (drop-in upgrades for the forecaster)
# pandas-ta>=0.3.14b       # 130+ indicators
# scikit-learn>=1.3.0      # classical ML models
